import VerifyUser from "../../reusuableMethods/verifyUser.js";

const s3 = require("../../../../lib/db/awss3Connect.js");

export async function POST(req) {
    // upload a new name, username, or profile picture
    // req will contain the attributes to be uploaded
    
    
}

export async function PATCH(req) {
    // change name, username, profile picture, or password
}